# Secrets — the real .env must never be committed
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.pytest_cache/

# Runtime artifacts (never commit these)
logs/
*.sqlite3
.golive_confirmed
