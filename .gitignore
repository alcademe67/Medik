.env
__pycache__/
*.pyc
.venv/
venv/
journal.sqlite

# data/, reports/ and logs/ are NOT listed here: each carries its own
# .gitignore that ignores its contents while tracking its README, so the
# directories exist in a fresh clone and explain themselves.
