# Medik

This repository's previous contents were removed.

Earlier code remains in the git history if it is ever needed - commit
`95b04f3` is the last one that contained it:

```bash
git checkout -b restore 95b04f3
```

No credentials were ever committed to this repository: `.env` has been
gitignored since the first commit, and `.env.example` only ever held
empty placeholders.
